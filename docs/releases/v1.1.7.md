# Release Notes — v1.1.7

## Summary
Refines the payment confirmation flow to require explicit owner approval before a payment is marked as Paid (BCM-110).

## Changes

### Two-step Payment Confirmation Flow
```
Before:  Player → POST /confirm → Paid

After:   Player → POST /confirm → ConfirmationPending + owner notified
         Owner  → POST /approve → Paid
```

### New Enum Values
- `PaymentStatus.ConfirmationPending = 3`
- `SessionPlayerStatus.ConfirmationPending = 4`

### Updated Endpoint
- `POST /api/payment/session-player/{id}/confirm` — now sets status to `ConfirmationPending` and sends a `PaymentRecorded` notification to the session owner.

### New Endpoint
- `POST /api/payment/session-player/{id}/approve` — owner approves the pending payment; sets `PlayerPayment.PaidStatus = Paid`, `SessionPlayer.Status = Paid`, and records `PaidAt`.

## Migration
EF Core migration `AddConfirmationPendingStatus` generated (enum stored as string — no schema change required).

## Updated Files
- `Badminton_BE/Models/PlayerPayment.cs`
- `Badminton_BE/Models/SessionPlayer.cs`
- `Badminton_BE/Services/Interfaces/IPaymentService.cs`
- `Badminton_BE/Services/PaymentService.cs`
- `Badminton_BE/Controllers/PaymentController.cs`
