{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",


  /*
  Example MySQL DefaultConnection. Replace values with your database credentials.
  Copy into appsettings.Development.json for development-only overrides if desired.
*/
  "ConnectionStrings": {
    // DO NOT store production secrets in source control.
    // Provide the real connection string via environment variable `ConnectionStrings__DefaultConnection`.
    // Example format (set as EB/Render env var or secrets):
    // "server=your-db-endpoint;port=3306;database=BadmintonDb;user=DB_USER;password=DB_PASS"
    "DefaultConnection": "server=localhost;port=3306;database=BadmintonDb;user=root;password=Mun1401@"
  },
  "Jwt": {
    "Issuer": "Badminton_BE",
    "Audience": "Badminton_FE",
    "Secret": "CHANGE_THIS_TO_A_LONG_RANDOM_SECRET_AT_LEAST_32_CHARACTERS",
    "ExpirationMinutes": 120
  }
}